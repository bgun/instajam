App.Views.TrackView = Backbone.View.extend({

  id: "track-view",

  tagName: "div",

  events: {
  },

  initialize: function() {
    console.log(this.options.songId);
  },

  render: function() {
  }

});
